//! Builder placeholder for `tag` (equation labels). Mirrors upstream
//! `functions/tag.ts` — no parser-side handler. Real builders land in
//! Phase 6.

use crate::define_function::FunctionSpec;

pub const SPECS: &[FunctionSpec] = &[];
