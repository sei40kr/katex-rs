/target
/result
/.direnv
/.pre-commit-config.yaml
/node_modules
/.npm-cache
/.npmrc
/.cargo-home
/.cache
/.sccache
/.gnupg
/katex-*.tgz
/package

# Local demo output regenerated from the `mathml_demo` example —
# never commit, since a snapshot would silently drift from the
# current renderer.
/examples/web/index.html

